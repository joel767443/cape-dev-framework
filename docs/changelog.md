# Changelog

This project doesn’t currently ship a maintained, user-facing changelog in the repository root.

If you want, we can:

- Start a `CHANGELOG.md` at the repo root (Keep a Changelog format)
- Or link this page to releases/tags instead

